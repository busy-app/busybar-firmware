# External images

External images are put on the storage and are available under the `/ext/apps_assets` device directory.

# Format support

Only PNG source images are supported. Before uploading to the device, they are converted into the LVGL binary format, and could be used both in `Image` widgets and `lv_image` objects.
