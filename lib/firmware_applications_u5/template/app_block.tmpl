    {
        .app = {{ app.entry_point }},
        .name = "{{ app.name }}",
        .appid = "{{ app.appid }}",
        .stack_size = {{ app.stack_size }},
        .flags = {{ '|'.join(f"FlipperInternalApplicationFlag{flag}" for flag in app.flags) }},
    }
