Import("env")

env.AppendUnique(
    CPPPATH=[
        Dir("drivers").srcnode(),
    ],
)

libenv = env.Clone(FW_LIB_NAME="drivers")
libenv.ApplyLibFlags()

sources = libenv.Glob("drivers/*.c", source=True)

lib = libenv.StaticLibrary("${FW_LIB_NAME}", sources)
libenv.Install("${LIB_DIST_DIR}", lib)
Return("lib")
