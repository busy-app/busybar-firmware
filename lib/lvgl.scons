Import("env")

env.AppendUnique(
    CPPPATH=[
        Dir("lvgl_gui").srcnode(),
        Dir("lvgl_gui/src").srcnode(),
    ],
    CPPDEFINES=[
        "LV_CONF_INCLUDE_SIMPLE",
    ],
)

libenv = env.Clone(FW_LIB_NAME="lvgl")
libenv.ApplyLibFlags()

libenv.AppendUnique(
    CPPFLAGS=[
        "-Wno-redundant-decls",
    ]
)

sources = []
sources += libenv.Glob("lvgl_gui/src/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/core/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/display/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/draw/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/draw/sw/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/draw/sw/blend/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/font/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/indev/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/layouts/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/layouts/*/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/libs/bin_decoder/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/misc/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/misc/cache/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/stdlib/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/stdlib/clib/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/osal/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/themes/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/themes/*/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/tick/*.c", source=True)
sources += libenv.Glob("lvgl_gui/src/widgets/*/*.c", source=True)

lib = libenv.StaticLibrary("${FW_LIB_NAME}", sources)
libenv.Install("${LIB_DIST_DIR}", lib)
Return("lib")
