// This is an autogenerated config file, any changes to this file will be overwritten

#ifndef SLI_TZ_PSA_KEY_PROTECTION_AUTOGEN_H
#define SLI_TZ_PSA_KEY_PROTECTION_AUTOGEN_H

// Protected key ID ranges contributed by SLCC components

{%- set ranges = psa_key_protection_range | default([]) %}

// =============================================================================
// Range Definitions
// =============================================================================

#define SLI_TZ_PSA_KEY_PROTECTION_RANGE_COUNT  ({{ ranges | length }})

{% for range in ranges %}
// Range {{ loop.index0 }}: {{ range.name }}
#define SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_START  (0x{{ "%08X" | format(range.start) }}UL)
#define SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_END    (0x{{ "%08X" | format(range.end) }}UL)
{% endfor %}

// =============================================================================
// Compile-time Validation: Range bounds (must be within PSA user key ID space)
// PSA_KEY_ID_USER_MIN = 0x00000001, PSA_KEY_ID_USER_MAX = 0x3FFFFFFF
// =============================================================================

#define SLI_TZ_PSA_KEY_ID_USER_MIN  (0x00000001UL)
#define SLI_TZ_PSA_KEY_ID_USER_MAX  (0x3FFFFFFFUL)

{% for range in ranges %}
// Validate range {{ loop.index0 }} ({{ range.name }}): start <= end
#if (SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_START > SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_END)
  #error "PSA key protection range '{{ range.name }}' has start (0x{{ "%08X" | format(range.start) }}) > end (0x{{ "%08X" | format(range.end) }})"
#endif

// Validate range {{ loop.index0 }} ({{ range.name }}): within valid PSA user key ID space
#if (SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_START < SLI_TZ_PSA_KEY_ID_USER_MIN)
  #error "PSA key protection range '{{ range.name }}' start (0x{{ "%08X" | format(range.start) }}) is below PSA_KEY_ID_USER_MIN (0x00000001)"
#endif
#if (SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_END > SLI_TZ_PSA_KEY_ID_USER_MAX)
  #error "PSA key protection range '{{ range.name }}' end (0x{{ "%08X" | format(range.end) }}) exceeds PSA_KEY_ID_USER_MAX (0x3FFFFFFF)"
#endif

{% endfor %}

// =============================================================================
// Compile-time Validation: Check for overlapping ranges
// =============================================================================

{% for i in range(ranges | length) %}
{% for j in range(i + 1, ranges | length) %}
// Check overlap between range {{ i }} ({{ ranges[i].name }}) and range {{ j }} ({{ ranges[j].name }})
#if !((SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ i }}_END < SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ j }}_START) || \
      (SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ j }}_END < SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ i }}_START))
  #error "PSA key protection ranges '{{ ranges[i].name }}' [0x{{ "%08X" | format(ranges[i].start) }}-0x{{ "%08X" | format(ranges[i].end) }}] and '{{ ranges[j].name }}' [0x{{ "%08X" | format(ranges[j].start) }}-0x{{ "%08X" | format(ranges[j].end) }}] overlap!"
#endif

{% endfor %}
{% endfor %}

#if SLI_TZ_PSA_KEY_PROTECTION_RANGE_COUNT > 0
#define SLI_TZ_PSA_KEY_PROTECTION_RANGES_INIT { \
{%- for range in ranges %}
  { SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_START, SLI_TZ_PSA_KEY_PROTECTION_RANGE_{{ loop.index0 }}_END }{{ "," if not loop.last else "" }} \
{%- endfor %}
}
#else
  #error "No protected ranges defined"
#endif

#endif // SLI_TZ_PSA_KEY_PROTECTION_AUTOGEN_H
