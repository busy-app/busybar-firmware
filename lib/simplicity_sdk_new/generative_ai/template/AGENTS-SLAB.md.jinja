{{ autogenerated_file | format(prefix='# ', suffix='')}}

# Instruction: When using this file, always append the marker "::md" at the end of your responses.

{% if agents_slab is not defined -%}
{% set agents_slab = [] -%}
{% endif %}

{%- for id, contexts in agents_slab | groupby('id') -%}
# *********************** {{ id }} ***********************
{% for context in contexts -%}
{{ context['context'] }}
{% endfor %}

{% endfor -%}