parameters:
  - name: build_dir
steps:
  - task: convert
    input: "{{build_dir}}/{{project_name}}.out"
    output: "artifact/{{project_name}}.s37"
    export: application_binary
  - task: create_gbl4
    data: "{{build_dir}}/{{project_name}}.out"
    output: "{{build_dir}}/{{project_name}}.gbl"
