App(
    appid="wifi_per_test",
    name="WiFi PER Test",
    apptype=FlipperAppType.APP,
    order=80,
    entry_point="wifi_per_test_app",
    requires=["gui"],
    stack_size=2 * 1024,
    targets=["f20"],
    sources=[
        "wifi_per_test.c",
        "helpers/wifi_per_cli.c",
    ],
)
