# Doxygen Documentation

This directory contains the developer documentation source files.

To view the developer documentation, visit the [ready to read version](#) or run `./fbt doxy` from the repository root directory to build it locally.
