# FBT layers

This directory contains layers for FBT (Flipper Build Tool).

- `fbtng` - the main module, contains the FBT implementation
- `core_libs` - `Furi` core libraries, OS primitives etc
- `freertos` - the `FreeRTOS` operating system kernel
